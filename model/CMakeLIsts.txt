cmake_minimum_required(VERSION 3.10)

add_library(model model.cxx mesh.cxx)

target_link_libraries(model PUBLIC gl_wrapped_lib ${proj_flag})

target_include_directories(model PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

